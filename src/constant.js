const BD_NAME = "xicomProject"
const DB_URI = process.env.DB_URI

const CORS_ORIGINE = process.env.CORS_ORIGINE

const PORT = process.env.PORT

module.exports = {
    BD_NAME,
    DB_URI,
    CORS_ORIGINE,
    PORT,
}
